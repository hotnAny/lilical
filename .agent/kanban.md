## to-do

## back-log

## done
- study the sketch in refs/ and its annotations; then compile a spec.md. let me know if anything is not legible or lacking specifics. → spec.md (2026-09-23; all questions settled)
- build it! → Astro app, works locally (2026-09-23); deploy steps in README.md
- use full names for each day in the week view (2026-09-23)
- the dot next to each day in the week view looks like a radio button. perhaps use a dotted contour and make it gray by default? (2026-09-23)
- use a gear icon for settings (2026-09-23)
- [?] when using it locally, the data still writes to lili-cal? → no: local dev writes only to .data/lilical.json; lilical-data has just its initial commit (2026-09-23)
- remove the important flag for events. all events by default should be displayed as a banner (2026-09-23)
- add another attribute for each list about when it will show up: weekday-only, weekend only, or all days. (2026-09-23)
- modify the adding event pop-up: default the start and end dates are the day that was clicked but i can modify it make a multi-day event without having to repeat the same adding tasks (2026-09-23)
- on the settings, make it possible to arrange the order of the added lists (except for a la carte and misc) and on the display, the lists will show up in that order: from left to right and from top to bottom (2026-09-23)
- instead of having start/end dates for a list, simply add a flag to show or hide it, which might trigger some other changes needed--just let me decide on them → decided: list shows where it has items; hidden = gone everywhere, items kept; ↑↓ reorder (2026-09-23)
- the ui elements on the event popup is spatially disorganized (see the screenshot) (2026-09-23)
- below the week view's next previous buttons, add a short cut to jump to "Today" (2026-09-23)
- the arrow up down buttons on settings page look weird. let's implement drag handle instead (2026-09-23)
- on settings page: use a single eye (toggle between full vs. half opacities) icon to replace checkbox+show (2026-09-23)
- on settings page: use a garbage bin icon instead of delete (2026-09-23)
- the today shortcut should be one the same row as the prev next week buttons (2026-09-23)
- instead of banner. display the event like a tag to the right of the date on the main pane (2026-09-23)
- if i paste a link in a to-do item, display it as a link icon rather than showing the full url (2026-09-23)
- i want to use a lily flower as the icon--can you create a svg that renders a simple sketch? → public/favicon.svg (2026-09-23)
- add the icon to the left of the lilical name at the top left of the screen (2026-09-23)
- add a handle to the left of each to-do item for dragging them to a different day, to a different list on the same day, and to rearrange their order on the current list (2026-09-23)
- each to-do item has its sown detailed "setting" (i want to use a different icon than gears), which is a popup the display its textual label, link (if there is any), and notes (optional), and recurring-ness (use a better word, see next item for details) → ⋯ icon; "Repeat" (2026-09-23)
- i want to set an to-do item as recurring by clicking on these circles that represent days of a week (the days where the list won't show should not be selectable). see the screenshot as an example → decided: no red flag for missed repeats; drag/× act on one day; delete-all in popup (2026-09-23)
- the repeat circles and the letters within should be smaller and should appear more muted by default (i.e., no repetition) (2026-09-23)
- for each to-do item, move the delete option to the pop-up. (2026-09-23)
- for to-do item, show the recurring icon at the beginning before its text label (2026-09-23)
- if i change an item from repeated to un-repeated, the ones up to the date the changes takes place will still exist but none will appear in the future onwards; similar logic for changing an item from un-repeated to repeated → also applies to changing weekdays; new series starts the day after (2026-09-23)
- instead of "remove this day" and "delete all days", just show a single "Delete", clicking which will then display something that prompts the user to confirm deleting today only or all days (2026-09-23)
- past to-do items that didn't get checked off will turn red → one-offs only; repeating items stay unflagged (2026-09-23)
- for non-repeated items, need to warn user when they click delete but having them click again to confirm deletion (2026-09-23)
- in settings, add an option to automatically move previously-unchecked items to the current day, same list, shown as red → skips repeating, checked, hidden-list items (2026-09-23)
- update the calendar view: a week starts with sunday → month grid, week strip, and Repeat circles all run Sunday→Saturday; stored weekday numbers unchanged (2026-09-24)
- vertically stack custom lists based on their order. keep the current width so the entire UI can be narrower (i.e., leaving some space on the right side of the screen, which i have not figured out how to make use of yet) → one 290px column + 270px fixed column, empty space to the right (2026-09-24)
- chrome has some web-based standalone app that runs outside of the browser. is there a way to run this app that way? → yes: made it an installable web app (manifest + icons); Chrome shows "Install lilical"; steps in README.md (2026-09-24)
- there is still empty space on the right. we should remove it and have the window scale to the contents → installed app now fits the window to the content (844px) on every launch, after load; replaces the first-launch-only fit, which Chrome could ignore and never retried (2026-09-24)
